[project]
name = "prism"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "raneemralju@hotmail.com", email = "264697320+raneemralju@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "langchain>=1.3.15",
    "langchain-openai>=1.4.3",
    "langgraph>=1.2.11",
    "pydantic>=2.13.4",
    "uvicorn>=0.52.1",
]

[project.scripts]
prism = "prism:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
